# 第三方 API 配置示例
# 复制此文件为 local.properties 并填入真实的 API 密钥
# 注意：不要将 local.properties 提交到 Git

# 阿里云配置
aliyun.access.key.id=your-aliyun-access-key-id
aliyun.access.key.secret=your-aliyun-access-key-secret

# 腾讯云配置
tencent.secret.id=your-tencent-secret-id
tencent.secret.key=your-tencent-secret-key

# 百度 AI 配置
baidu.api.key=your-baidu-api-key
baidu.secret.key=your-baidu-secret-key

# SendGrid 邮件服务配置（必需，用于注册时的邮箱验证）
# 配置步骤：
# 1. 注册 SendGrid 账号：https://sendgrid.com/
# 2. 创建 API Key：Settings -> API Keys -> Create API Key（确保有 "Mail Send" 权限）
# 3. 验证发件人邮箱：Settings -> Sender Authentication -> 验证您的邮箱
# 4. 将下面的占位符替换为实际值
# 5. 重新构建项目（Build -> Rebuild Project）
#
# 注意：此文件不会被提交到 Git，每个开发者需要在自己的 local.properties 中配置
sendgrid.api.key=your-sendgrid-api-key-here
sendgrid.from.email=your-verified-email@example.com
